# Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values

# API Configuration
# Uses relative /api path which works for both:
# - Local development (Vite proxies to localhost:5000)
# - Production (Vercel routes through /api functions)
VITE_API_URL=/api

